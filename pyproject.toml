[project]
name = "letta-client-bot"
version = "1.2.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10,<3.14"
dependencies = [
  "aiogram>=3.30.0",
  "elevenlabs>=2.64.0",
  "gel>=3.1.0",
  "google-genai>=2.0.0",
  # openai 3.x ships httpx2; httpx itself is imported directly by generate_image.py
  "httpx>=0.28",
  "letta-client==1.12.1",
  "mistune>=3.3.4",
  "openai>=3.1.0",
  "pydantic>=2.13.4",
  "pydantic-settings>=2.15.0",
]

[dependency-groups]
dev = ["ruff>=0.16.3", "mypy>=2.3.1", "pytest>=9.1.1"]

[tool.ruff]
line-length = 92
target-version = "py313"
exclude = [
  ".git",
  ".venv",
  "__pycache__",
  "build",
  "dist",
  "devscripts/",
  "letta_bot/queries/*_async_edgeql.py",
  "letta_bot/custom_tools/",
  # ruff 0.16 formats Python blocks inside Markdown; keep docs hand-written.
  "*.md",
]

[tool.ruff.lint]
select = [
  "E",   # pycodestyle errors
  "F",   # pyflakes
  "W",   # pycodestyle warnings
  "I",   # isort
  "B",   # flake8-bugbear
  "UP",  # pyupgrade
  "ANN", # flake8-annotations
  "C4",  # flake8-comprehensions
  "SIM", # flake8-simplify
]
ignore = []
fixable = ["ALL"]
unfixable = []

[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401"]   # Allow unused imports in __init__.py
"tests/*" = ["ANN"]        # Don't require type annotations in tests
"devscripts/*" = ["ANN"]   # Don't require type annotations in devscripts

[tool.ruff.lint.isort]
known-first-party = ["letta_client_bot", "md_tg"]
combine-as-imports = true
force-sort-within-sections = true

[tool.ruff.format]
quote-style = "single"
indent-style = "space"
skip-magic-trailing-comma = false
line-ending = "auto"

[tool.mypy]
python_version = "3.13"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
check_untyped_defs = true
disallow_any_generics = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_unreachable = true
strict_equality = true
exclude = [
  "letta_bot/queries/.*_async_edgeql\\.py$",
  "devscripts/",
  "letta_bot/custom_tools/",
  "tests/",
]
plugins = ['pydantic.mypy']

[[tool.mypy.overrides]]
module = ["letta_client.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["google.genai.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "letta_bot.queries.*"
follow_imports = "skip"
disallow_untyped_defs = false
